.uk-margin-top.uk-container
  h2 外部IdP設定 新規作成

  .uk-margin-top.uk-card.uk-card-default.uk-card-body
    = form_with model: @oauth_provider, url: ruler_area_tenant_oauth_providers_path(@tenant_id), method: :post do |f|
      = render 'form', f: f
