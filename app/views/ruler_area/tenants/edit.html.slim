.uk-margin-top.uk-container
  h2 テナント編集

  .uk-margin-top.uk-card.uk-card-default.uk-card-body
    = form_with model: @tenant, url: ruler_area_tenant_path, method: :put do |f|
      = render 'form', f: f
