.uk-margin-top.uk-container
  h2 OAuth Application 編集

  .uk-margin-top.uk-card.uk-card-default.uk-card-body
    = form_with model: @oauth_application, url: ruler_area_tenant_oauth_application_path(@tenant_id), method: :put do |f|
      = render 'form', f: f
