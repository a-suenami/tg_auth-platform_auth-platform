.uk-margin-top.uk-container
  h2 OAuth Application 編集

  .uk-margin-top.uk-card.uk-card-default.uk-card-body
    = form_with model: @oauth_application, url: admin_area_oauth_application_path, method: :put do |f|
      = render 'form', f: f
