.uk-margin-top.uk-container
  h2 メールテンプレート新規作成

  .uk-margin-top.uk-card.uk-card-default.uk-card-body
    = form_with model: @email_template, url: admin_area_email_templates_path, method: :post do |f|
      = render 'form', f: f
