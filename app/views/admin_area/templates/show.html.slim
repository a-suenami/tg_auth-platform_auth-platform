.uk-margin-top.uk-container
  / Breadcrumb
  = render 'breadcrumb'

  / Template Name Section
  = render 'template_header'

  / Distribution Templates Section
  = render 'distribution_templates'

/ Modal for Mail Template Draft
= render 'mail_template_modal'
