ja:
  activerecord:
    models:
      oauth_application: OAuth Application
    attributes:
      oauth_application:
        id: ID
        tenant_id: テナント
        name: アプリケーション名
        uid: Client ID
        secret: Client Key
        redirect_uri: リダイレクトURL
        scopes: スコープ
        confidential: confidential
        enable_client_credential_flow: Admin APIの利用
        enable_push_event: 変更通知Event発行
        require_two_factor_auth_by_sms: SMS 2fa有効化
  enumerize:
    oauth_application:
      template_type:
        registered: 登録完了メール
        email_address_verification: メールアドレス確認メール
        password_reset: パスワードリセットメール
        email_address_change: メールアドレス変更確認メール
